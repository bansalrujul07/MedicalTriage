apiVersion: v1
kind: Service
metadata:
  name: medicaltriage-api
spec:
  type: ClusterIP
  selector:
    app: medicaltriage-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
